name: Domain CI

on:
  push:
    branches: ["main"]
    paths:
      - rsconcept/domain/**
      - package.json
      - package-lock.json
      - .github/workflows/domain.yml
  pull_request:
    branches: ["main"]
    paths:
      - rsconcept/domain/**
      - package.json
      - package-lock.json
      - .github/workflows/domain.yml

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04

    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          cache-dependency-path: package-lock.json
      - name: Install workspaces (root)
        run: npm ci
      - name: Typecheck
        run: npm run typecheck -w @rsconcept/domain
      - name: Test
        run: npm test -w @rsconcept/domain
      - name: Build
        run: npm run build -w @rsconcept/domain
  notify-failure:
    permissions: {}
    runs-on: ubuntu-latest
    needs: build
    if: failure()
    steps:
      - name: Send Telegram Notification
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="❌ Domain build failed! Repository: ${{ github.repository }} Commit: ${{ github.sha }}"
