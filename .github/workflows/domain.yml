name: Domain CI

on:
  push:
    branches: ["main"]
    paths:
      - rsconcept/domain/**
      - .github/workflows/domain.yml
  pull_request:
    branches: ["main"]
    paths:
      - rsconcept/domain/**
      - .github/workflows/domain.yml

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04

    strategy:
      matrix:
        node-version: [24.x]

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          cache-dependency-path: rsconcept/domain/package-lock.json
      - name: Install dependencies
        working-directory: rsconcept/domain
        run: npm ci
      - name: Typecheck
        working-directory: rsconcept/domain
        run: npm run typecheck
      - name: Lint
        working-directory: rsconcept/domain
        run: npm run lint
      - name: Test
        working-directory: rsconcept/domain
        run: npm test
      - name: Build
        working-directory: rsconcept/domain
        run: npm run build
  notify-failure:
    permissions: {}
    runs-on: ubuntu-latest
    needs: build
    if: failure()
    steps:
      - name: Send Telegram Notification
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="❌ Domain build failed! Repository: ${{ github.repository }} Commit: ${{ github.sha }}"
