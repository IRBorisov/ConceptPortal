import { useTx } from '@/i18n';

import { TextURL } from '@/components/control';
import { external_urls } from '@/utils/constants';

export function HelpAssistantRu() {
  const tx = useTx();
  return (
    <>
      <h1>{tx('tx.ai')}</h1>
      <p>
        ИИ помощник предназначен для работы с предметной областью и концептуальными схемами с помощью генеративных LLM
        чат-ботов.
      </p>

      <h2>Запросы LLM</h2>
      <p>
        Пользователи могут создавать шаблоны запросов, используя встроенные переменные {`{{название-переменной}}`} или
        выбрав из списка доступных шаблонов.
      </p>
      <p>
        При применении шаблона формируется запрос для LLM, в котором все переменные заменены на соответствующие значения
        из текущего контекста Портала.
      </p>
      <p>
        Подключение чат-бота напрямую не поддерживается. Полученный запрос предполагается отправлять во внешний LLM чат
        вручную.
      </p>

      <h2>rstool — библиотека для внешних ИИ-агентов</h2>
      <p>
        Пакет <TextURL text='@rsconcept/rstool' href={external_urls.git_rstool} /> — библиотека для внешних ИИ-агентов,
        работающих с концептуальными схемами Портала. Она обеспечивает инкрементальное построение RSForm, анализ
        выражений RSLang, диагностику, моделирование и вычисление. Библиотека может использоваться напрямую из кода или
        через stdio JSON-обёртку <code>rstool-wrapper</code>.
      </p>
      <p>
        Для агентов публикуется навык <code>rstool-helper</code> с руководством по языку RS и типовым сценариям работы.
        Установка и примеры — в <TextURL text='документации rstool' href={external_urls.git_rstool} />.
      </p>

      <h2>MCP — подключение агента</h2>
      <p>
        Пакет <TextURL text='@rsconcept/rstool-mcp' href={external_urls.git_rstool_mcp} /> — адаптер протокола Model
        Context Protocol (MCP) для rstool. Он предоставляет полный контракт rstool (сессии, конституенты, анализ,
        диагностика, моделирование) в виде MCP-инструментов поверх stdio, чтобы подключить rstool к Cursor, Claude
        Desktop и другим MCP-совместимым средам.
      </p>
      <p>
        Запуск: <code>npx rstool-mcp</code>. Пример конфигурации для Cursor и список инструментов — в{' '}
        <TextURL text='документации rstool-mcp' href={external_urls.git_rstool_mcp} />.
      </p>
    </>
  );
}
