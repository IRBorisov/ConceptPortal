import { LinkTopic } from '../../components/link-topic';
import { HelpTopic } from '../../models/help-topic';

export function HelpConceptRSModel() {
  return (
    <div className='text-justify'>
      <h1>Концептуальная модель</h1>
      <p>
        <b>Концептуальная модель</b> объединяет <LinkTopic text='концептуальную схему' topic={HelpTopic.CC_SYSTEM} /> и{' '}
        <em>интерпретацию</em> её понятий в предметной области. Теоретически это пара (схема, интерпретация): схема определяет сигнатуру и определения, модель назначает базовым понятиям конечные носители (множества, структурированные данные) и фиксирует, какие объекты считаются значениями термов и какие формулы истинны в данной ситуации. Производные понятия не задаются отдельно: их значения выводятся из определений схемы при выбранной интерпретации базовых понятий.
      </p>

      <p>
        Концептуальная схема отвечает на вопрос «что и как определено», модель — на вопрос «какие значения принимают эти понятия сейчас». Вместе они позволяют проверять согласованность данных с определениями, проводить эксперименты с выражениями и автоматически пересчитывать значения по графу зависимостей между конституентами.
      </p>

      <h2 className='mt-6 text-lg font-semibold'>Как устроена модель</h2>
      <p>
        Модель хранит для каждой конституенты текущее значение как структурированные данные (числа, кортежи, конечные множества — упорядоченные наборы элементов). Базисные множества — это списки элементов. Родовые структуры формируются из этих элементов. Значения остальных понятий, при необходимости, вычисляются по определениям в топологическом порядке зависимостей. Функциональные конституенты работают как подпрограммы: при вызове создаётся локальный контекст аргументов, а тело вычисляется тем же способом, что и любая формула.
      </p>
      <p>
        Выражения языка родов структур вычисляются обходом синтаксического дерева: глобальные имена берутся из контекста модели, кванторы и конструкции перебирают элементы уже построенных конечных множеств, рекурсивные определения обрабатываются итеративно до сходимости. Вкладка <LinkTopic text='вычисления выражения' topic={HelpTopic.UI_RSMODEL_EVALUATOR} /> позволяет проверить любую формулу в текущем контексте модели.
      </p>

      <h2 className='mt-6 text-lg font-semibold'>Ограничения вычислений</h2>
      <p>
        Используется <em>конечная</em> семантика: все множества в памяти конечны и явно перечислены. Бесконечные области (например, множество всех целых) не поддерживаются. Глубокая вложенность переборов или рекурсии, а также большие декартовы произведения увеличивают количество шагов; при превышении лимита вычисление прерывается ошибкой. Если произведение мощностей факторов слишком велико, декартово произведение не строится.
      </p>
      <p>
        Оператор булеана (<code className='text-sm'>B(X)</code>, множество всех подмножеств X) теоретически даёт 2<sup>|X|</sup> элементов. В системе оно <em>материализуется полностью</em>: перебираются и сохраняются все подмножества. Даже при умеренном размере X это экспоненциально растёт по памяти и времени; при вложении булеанов рост еще быстрее. Поэтому введён жёсткий предел на мощность аргумента булеана: если X слишком большое, построение <code className='text-sm'>B(X)</code> отклоняется, и выражение нужно переписать.
      </p>
      <p>
        Вывод: модель служит для исполнимой проверки определений на реальных конечных данных.
      </p>
    </div>
  );
}
