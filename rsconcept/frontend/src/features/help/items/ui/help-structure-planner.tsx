import { useTx } from '@/i18n';

import { isMac } from '@/utils/utils';

import { LinkTopic } from '../../components/link-topic';
import { HelpTopic } from '../../models/help-topic';

export function HelpStructurePlanner() {
  const tx = useTx();
  return (
    <>
      <h1>{tx('tx.concept.expandStructure.noun')}</h1>
      <p>
        Полноэкранный диалог для работы со структурой типизации выбранной{' '}
        <LinkTopic text='конституенты' topic={HelpTopic.CC_CONSTITUENTA} />. По дереву типизации строится граф операций
        (малые и большие проекции, множество-сумма). Узлы соответствуют структурным элементам'; для каждого отображается
        формальное определение, обозначение (имя) и <LinkTopic text='термин' topic={HelpTopic.CC_CONSTITUENTA} />.
      </p>
      <p>
        Диалог открывается из вкладки «Понятие» по кнопке «Типизация» (если для конституенты есть структура) или из меню
        схемы по команде «Порождение структуры».
      </p>

      <h2>{tx('tx.graph')}</h2>
      <ul>
        <li>клик по узлу выбирает элемент структуры</li>
        <li>если для него уже есть конституента — она подставляется, иначе предлагается имя для новой</li>
        <li>корень графа — открытая конституента (для терм-функции — тип результата)</li>
        <li>
          если конституента <LinkTopic text='порожденная' topic={HelpTopic.CC_RELATIONS} />, то корень — структура
          основания
        </li>
      </ul>

      <h2>Верхняя панель</h2>
      <ul>
        <li>слева — формальное определение выбранного элемента</li>
        <li>в поле термина поддерживаются текстовые отсылки</li>
        <li>
          в поле термина: <kbd>{isMac() ? 'Cmd + S' : 'Ctrl + S'}</kbd> — сохранить термин или создать конституенту
        </li>
      </ul>
    </>
  );
}
