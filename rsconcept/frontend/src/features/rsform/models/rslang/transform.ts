/**
 * Module: Transforming AST to a simple form.
 */

import { type AstNode } from '@/utils/parsing';

/** Normalizes AST to a simple form. */
export function normalizeAST(ast: AstNode) {
  console.log(ast);
}